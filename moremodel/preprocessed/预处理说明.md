# 数据预处理说明（自动生成）

## 一、数据概况
- 原始文件：`/Users/samuels/riskpredict/moremodel/test_data.xlsx`
- 样本量：**160** 例
- 原始特征数：**11016** 个
- 结局「是否 QFR≤0.8」：0（未发病）**89** 例，1（发病）**71** 例

## 二、为何不能直接用 1 万多个指标建模？
本数据属于 **小样本、超高维（p>>n）**：特征数远大于样本数。若不做筛选，
模型会严重过拟合，论文审稿人也难以接受。因此采用「质控 → 与结局相关预筛 → 去共线 → 多重比较校正 → 标准化」的流程。

## 三、预处理步骤（写入论文时可略作润色）
1. **质控**：剔除缺失率 > 30% 的变量、近零方差变量、完全重复变量；
2. **与结局关联的预筛**：按与结局的点二列相关系数，保留前 **2000** 个变量；
3. **共线性削减**：在预筛子集上，对 |Pearson r| ≥ **0.95** 的变量簇聚类，每簇保留与结局相关最强者（剩余 **490** 个）；
4. **多重比较与入选**：Benjamini–Hochberg FDR（α=0.05），结合互信息，最终保留 **30** 个变量；
5. **标准化**：对入选变量做 Z-score，得到 `X_preprocessed.csv`。

## 四、最终入选特征
  - face wenli variance904
  - face wenli variance544
  - face wenli variance686
  - right ear wenli variance398
  - left ear wenli mean480
  - left ear wenli variance358
  - face wenli variance648
  - face wenli variance179
  - face wenli variance896
  - left ear wenli variance512
  - face wenli variance1163
  - face wenli variance479
  - right ear wenli variance1303
  - left ear wenli variance1400
  - face wenli variance895
  - face wenli mean464
  - right ear wenli variance1182
  - left ear wenli mean1280
  - left ear wenli variance472
  - left ear wenli mean222
  - left ear wenli variance214
  - right ear wenli variance1294
  - face wenli variance1024
  - face wenli variance128
  - right ear wenli variance1464
  - right ear wenli variance1351
  - left ear wenli variance496
  - face wenli variance526
  - left ear wenli variance1184
  - left ear wenli variance488

## 五、输出文件说明
| 文件 | 含义 |
|------|------|
| `X_preprocessed.csv` | 标准化后的特征矩阵（**后续建模请用此文件 + y.csv**） |
| `X_selected_unscaled.csv` | 入选特征的原始数值（未标准化） |
| `y.csv` | 结局列 |
| `univariate_scores.csv` | 全部特征与结局的单变量相关与 p 值 |
| `selection_detail.csv` | 去共线后子集上的 FDR、互信息、是否入选 |
| `preprocess_report.json` | 机器可读的完整参数与结果 |

## 六、下一步：预测模型
在 `moremodel` 目录运行（交叉验证在每一折**内部**重新做特征筛选，避免信息泄漏）：

```bash
python run_thesis_pipeline.py --step model
```

或一键预处理 + 建模：

```bash
python run_thesis_pipeline.py --step all
```

## 七、重要提示
- 阳性事件约 71 例；按 EPV≈10 的经验法则，未正则化模型同时纳入的变量宜 ≤ **7** 个；本流程保留 30 个并建议建模时使用 **LASSO 正则化**。
- 当前 `X_preprocessed.csv` 的筛选是在**全数据**上完成的，适合探索性分析；**正式报告模型性能**时请使用 `run_thesis_pipeline.py` 的折内筛选结果。
